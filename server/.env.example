PGHOST=localhost
PGPORT=5432
PGDATABASE=war_patterns
PGUSER=postgres
PGPASSWORD=postgres
PORT=3001
POLL_INTERVAL_MS=15000

# Comma-separated list of allowed CORS origins
# REQUIRED in production! Add your production domain(s) here.
# Example: ALLOWED_ORIGINS=https://example.com,https://www.example.com
# Default (development): http://localhost:5173,http://localhost:3001
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3001
